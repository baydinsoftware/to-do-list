<form action="{% url 'todolist:add' l.id %}" method="post">
    {{form.non_field_errors}}
    <div class="fieldWrapper">
        {{form.text.errors}}
        <label for="id_text">Item text:</label>
        {{form.text}}
    </div><div class="fieldWrapper">
        {{form.priority.errors}}
        <label for="id_priority">Item priority:</label>
        {{ form.priority }}
    </div><div class="fieldWrapper">
        {{form.completed.errors}}
        <label for="id_completed">Is item completed?</label>
        {{form.completed}}
    </div>
    <p><input type="submit" value="Add Item" /></p>
</form>
